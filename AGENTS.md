# Drudge AI 维护约定

本仓库主要由 AI 迭代。目标是用最小完整改动交付用户需要的行为，并让下一次任务能从当前事实继续工作。

## 阅读顺序与事实来源

1. 本文件：工作和数据边界。
2. [README.md](README.md)：已完成的里程碑、当前优先级和文档索引。
3. [docs/architecture.md](docs/architecture.md)：应用责任和数据契约。
4. 对应源码、调用方、测试和配置；用 `rg` 定位，不凭文档猜实现。
5. 按需读开发、消息、部署或数据库手册，不为小任务遍历整套运维材料。

用户当前决定定义目标与授权；源码和测试描述现行行为；文档解释设计与操作。冲突必须辨明并修正，不能把历史设计当成已上线事实，也不能用现行代码覆盖用户新决定。

## 产品与已确认决定

产品核心是“实时采集 → 处理 → 总结 → 可靠主动推送”。完整数字、时间、主体、来源、事件和原文链接保留在快照与 H5；图片用于快速了解事件。区分来源事实与模型推断，保留事件句中的计划、否认、尚未确认等限定，不为视觉紧凑改变事实。

当前已确认：

- 使用现有企业机器人“牛长婷”。用户最新决定：图片是快捷播报，每条先用事件句说清谁做了什么、关键变化，必要时附一句已有历史/背景；实体清单、详细正文与图谱深度探索留给详情和后续能力。此前“完整详情全部进入图片”的要求已被此决定替代；每条事件仍需进入消息，不恢复三条上限。克制字号、单一柔和色系，视觉强调集中在事件句。当前生产版本与本地迭代状态分别见部署和消息手册。
- 一个显式配置的收件人；没有默认用户、群聊、全员或广播 fallback。
- 简报和工作台统一使用 `drudge.microzj.com`；旧 `news.microzj.com` 不保留别名或跳转。
- 按用户要求采用 `public/none`，工作台页面和 API 不加账号密码。浏览器跨站操作检查、钉钉 API 凭据和收件人校验继续保留；不得自行重新加登录层。
- 工作台里程碑已完成；下一阶段重点见 README 和消息手册，不自行扩展成平台建设。

Neo4j、Web UI、钉钉通道、现有数据源和部署拓扑都属于受保护的当前能力。需要替换、删除或改变其角色时，先明确影响、迁移和回退；已有用户授权覆盖的动作不重复询问。

## 修改方式

- 先明确目标、非目标、不变量和验收；小任务各一句即可，使用已有工作记录或任务说明，不为每次修改新建文档。
- 改动放在负责该行为的包。跨包复用通过明确数据格式、HTTP/数据库边界或 `@drudge/common`，不跨包深层导入 `src/`。
- 优先修通用规则，并验证真实调用路径；不靠测试假结果、静默 fallback、吞错或特判一个样例制造成功。
- 不顺手改调度频率、消息发送条件、成本策略、数据格式、schema 或保留期。这些变化必须属于用户明确授权的范围。
- 新依赖、配置项、重试、抽象和服务必须有当前需求或已观察故障作为理由；不做无关重构、升级、格式化或清理。
- 保留用户和其他任务的已有改动。遇到冲突先定位所有权，不执行宽泛清理或覆盖。

配置统一通过 `shared/common/utils/env.js` 的加载与 `buildIngestConfig`、`buildGraphConfig`、`buildWebConfig` 构造；新配置同步 `env.example` 和测试。注释沿用代码惯例，用户交付文档默认中文。

## 数据与权限

以下都不是缓存或普通源码：`.env`、`data/`、`old_data/`、`neo4j/`、日志、`*.bak-*`、PM2 状态、容器卷、`.processed` 消费位点、失败新闻和持久化简报。

- 不把真实环境变量、密钥、完整 Webhook、私人新闻正文或用户 ID 写入源码、文档、日志、fixture、截图或 diff。
- 删除位点会重放历史文件；清理失败新闻会丢失重试证据。采集清理 API、Graph `db-clean*`、数据库清空、卷变更和生产数据删除需要独立、精确、可回滚的授权。
- 不运行宽泛 `git clean`、`git reset --hard` 或 `rm -rf`。Graph CLI 的 `db-health`、`db-stats`、`stats` 可能初始化 schema，不当作生产只读检查。
- 测试沿用现有禁网、临时目录和生产路径保护，mock 数据库、AI 与消息投递；不关闭 guard。
- commit、push、部署、生产重启和真实消息投递只在用户授权范围内执行。同一任务已有授权持续有效，不逐命令重复确认；新目标、更大影响或不可恢复动作需要重新说明。
- 启动 worker、`web-scheduler`、调用扫描/总结/机器人接口都可能产生业务副作用。**GET 不一定只读**：`/api/summary` 会调用 AI，并可能发送消息。

## 验证与交付

按[开发手册](docs/development.md)选择与改动影响相称的验证。代码变更完成后运行 `pnpm run verify`；UI 还需真实浏览器检查；纯文档检查链接、引用、命令语法与 diff，不为此运行生产操作。

交付前复核一次成功路径和一次失败/副作用路径。明确区分本地检查、模拟验证、GitHub 更新、生产生效和用户实际收到消息，未做的不能写成通过。

生产发布按[部署手册](docs/deployment.md)核对目标 SHA、现场容器、配置、回退点和验收。生产不直接编辑源码；Neo4j、卷和宿主机不属于普通应用发布范围。

## 文档维护

README 是接手入口，各主题主要文档的归属以 README 表格为准。行为变化时更新对应文档；可彻底改写过时内容，但先保留仍有效的事实并修正引用。

- 当前状态写清日期和证据；未来计划标为未实施；默认配置不等同生产配置。
- 决定记录解释“为什么”，当前行为与命令回链主要手册；被替代部分显式标明，不保留两套有效说明。
- 只有用户批准的重大架构/产品决定才需要新 ADR；普通修改不新建状态文件、交接文件、知识库或自动审计流程。
- `artifacts/` 可存放临时合同、模拟数据与验收记录，但重要结论必须进入现有正式文档，且不含秘密或私人正文。
- 最终说明结果、必要证据、实际同步范围和仍未验证的部分，保持简洁。

<!-- home-ingress:managed:start -->

## 家庭 HTTP 入口

本服务仍由当前仓库负责。对外提供 HTTP 服务时：

1. 先读当前仓库的 `ops/home-service.yaml`；
2. 再读 IH 工作区 `home-ingress/SERVICE-ONBOARDING.md`；
3. 用 Home Ingress 项目的 `bin/home-ingressctl check` 校验；
4. 用 `render` 生成本服务自己的 Caddy 路由；
5. 不为服务新增 iKuai 映射、ECS 公网端口、DNS、证书、RAM 或隧道。

入口契约：`home.microzj.com/v1`

<!-- home-ingress:managed:end -->
